{
  "name": "kaimple-desktop",
  "version": "0.2.5",
  "private": true,
  "description": "KaimPLE desktop app (Electron + Next.js)",
  "main": "electron/main.js",
  "author": "sfu-oss",
  "scripts": {
    "dev:renderer": "cd renderer && npm run dev",
    "dev:electron": "ELECTRON_DISABLE_SECURITY_WARNINGS=1 electron .",
    "dev": "concurrently \"npm:dev:renderer\" \"wait-on http://localhost:3000 && npm:dev:electron\"",
    "build:renderer": "cd renderer && npm run build && cd .. && node scripts/fix-paths.js",
    "build": "npm run build:renderer && electron-builder",
    "dist": "npm run build",
    "postinstall": "cd renderer && npm install"
  },
  "build": {
    "appId": "com.kaimple.desktop",
    "productName": "KaimPLE",
    "files": [
      "electron/**/*",
      "renderer/out/**/*",
      "package.json"
    ],
    "asar": true,
    "directories": {
      "buildResources": "assets"
    },
    "mac": {
      "target": "dmg",
      "category": "public.app-category.engineering"
    },
    "win": {
      "target": "nsis"
    },
    "publish": {
      "provider": "github",
      "owner": "sfu-oss",
      "repo": "plekaim-desktop",
      "private": true
    },
    "extraMetadata": {
      "main": "electron/main.js"
    }
  },
  "devDependencies": {
    "concurrently": "^8.2.2",
    "electron": "^30.0.0",
    "electron-builder": "^24.9.1",
    "wait-on": "^7.2.0"
  },
  "dependencies": {
    "electron-log": "^5.3.4",
    "electron-updater": "^6.3.0"
  }
}
