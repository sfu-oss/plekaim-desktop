/**
 * Post-build: no longer needed with custom app:// protocol
 * Kept as no-op for backwards compatibility with build script.
 */
console.log('fix-paths.js: using custom protocol, no path rewriting needed');
